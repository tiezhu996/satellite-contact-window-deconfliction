# gb-527 卫星地面站窗口冲突消解

请生成英文短名为 `satellite-contact-window-deconfliction` 的 Go 全栈 Web 项目。系统面向卫星运营规划人员，在离线数据中维护卫星、地面站和候选接触窗口，识别资源时间冲突并比较消解建议。系统不连接天线、射频设备或任务控制系统，不发送遥控指令，任何建议必须经人工复核后才可导出为规划记录。项目不做预约平台、工单、库存、订单、计费或通用日历产品。

## 项目主要需求

### 1. 核心实体

- `GroundStation`：字段至少包含 `id`、`station_code`、`name`、`latitude`、`longitude`、`antenna_count`、`supported_bands_json`、`slew_buffer_sec`、`station_status`、`version`；贯穿全栈。
- `SatelliteAsset`：字段至少包含 `id`、`satellite_code`、`name`、`orbit_class`、`supported_bands_json`、`priority_weight`、`minimum_contact_sec`、`asset_status`、`version`；只表达规划资产，贯穿全栈。
- `ContactWindow`：字段至少包含 `id`、`station_id`、`satellite_id`、`start_at`、`end_at`、`band`、`elevation_peak_deg`、`window_status`、`priority`、`locked`、`source_version`；时间和兼容性严格校验，贯穿全栈。
- `ConflictResolution`：字段至少包含 `id`、`conflict_key`、`window_ids_json`、`conflict_type`、`suggestions_json`、`selected_action`、`resolution_status`、`resolved_by`、`version`、`resolved_at`；保留算法建议和人工选择，贯穿全栈。

### 2. 核心页面

- `/stations` 地面站容量页：消费 `GroundStation`、`ContactWindow`，查看天线资源和窗口占用。
- `/satellites` 卫星需求页：消费 `SatelliteAsset`、`ContactWindow`，维护优先权和最短接触要求。
- `/windows` 窗口时间线：消费 `ContactWindow`、`GroundStation`、`SatelliteAsset`，筛选、锁定和提交候选窗口。
- `/conflicts` 冲突消解页：消费 `ConflictResolution`、`ContactWindow`，查看证据、建议并人工选择方案。
- `/audit` 审计页：消费 `ConflictResolution`、`SatelliteAsset`，比较规划版本和操作记录。

### 3. 差异算法与状态流

- 使用按开始时间排序的 sweep-line 算法检测同一地面站天线容量冲突、同一卫星重叠窗口、频段不兼容和不足最短时长；考虑窗口前后 `slew_buffer_sec`。
- 对冲突组生成可解释候选：保留高优先级、移动到同源备选窗口、分配到兼容地面站或标记人工处理；评分包含优先级损失、移动距离、接触时长和资源余量。
- 锁定窗口不可由算法自动移动；相同输入和权重必须产生稳定排序。算法只能建议，不得自动写回被选窗口。
- `ConflictResolution` 状态为 `detected -> proposed -> pending_review -> accepted | rejected`；接受时在一个事务内检查所有窗口版本并更新选择，版本变化返回冲突。

### 4. 横切关注点

- JWT/RBAC 角色 `scheduler`、`reviewer`、`admin`，贯穿用户角色、认证/授权中间件、路由、前端守卫和按钮显隐。
- 审计记录资源参数、窗口锁定、冲突检测、算法权重、建议和人工决策，含 request ID 与前后摘要。
- 全局错误处理统一时间范围、资源不兼容、版本冲突、非法状态和无权限；前端不能吞掉 409。
- 独立请求追踪、结构化访问日志、panic recovery、本地限流，日志不记录 JWT 或敏感备注全文。

### 5. 共享枚举、组件、hooks 与中间件

- `WindowStatus = candidate | submitted | locked | allocated | cancelled`：后端 `backend/internal/constants/window.go`，前端 `frontend/src/types/window.ts`。
- `ConflictType = station_capacity | satellite_overlap | band_mismatch | duration_shortfall | slew_buffer`：后端 `backend/internal/constants/conflict.go`，前端 `frontend/src/types/conflict.ts`。
- README 列明枚举在数据库、后端各层和前端 store/component/page 的全部位置。
- 共享组件：`WindowStatusBadge`（窗口/冲突页）、`ResourceTimeline`（站点/窗口页）、`ResolutionComparePanel`（冲突/审计页）。
- hooks：`useAuth`、`useConflictDetection`，统一权限和检测轮询。
- 中间件至少独立实现 `RequestID`、`AccessLog`、`Auth`、`RBAC`、`Recovery`、`RateLimit`。

### 6. 技术与规模红线

- Angular 17 + TypeScript + Angular CLI/esbuild + Angular Material + RxJS，时间线必须使用真实 API 数据。
- Go 1.22 + Gin + GORM + PostgreSQL，validator/v10、结构化日志、构造器注入、事务、`%w` 和优雅停机。
- 2500-4200 行功能 Go 代码、24-42 个功能 `.go` 文件且低于 5000 行，不计测试/vendor/生成代码/前端；禁止空壳或职责合并。
- API `/api/v1`，真实 `/healthz`、`/readyz`、统一响应/分页/错误码；区间检测、缓冲区、稳定评分和事务冲突需测试。
- 禁止 TODO、placeholder、随机建议、算法直接落地控制动作或 handler 直连数据库。

### 7. 文件结构强制清单

```text
./
├── go.work                 # use ./backend
├── docker-compose.yml
├── .env
├── .env.example
├── .gitignore
├── README.md
├── runtime_smoke.json
├── backend/
│   ├── go.mod
│   ├── Dockerfile
│   ├── cmd/server/main.go
│   └── internal/{config,model,dto,repository,service,handler,router,middleware,constants,scheduler,util}/
├── frontend/
│   ├── Dockerfile
│   ├── nginx.conf
│   └── src/app/{api,stores,types,components/common,hooks,pages,router,utils}/
└── output/execution.md
```

实体逐层分文件；区间检测、冲突分组、候选生成和评分分文件。严禁合并职责到单一文件。

`runtime_smoke.json` 只能是流水线启动 manifest，不得记录构建、API 或 Browser 验收。必须使用以下可解析内容，并实现 smoke 用 GORM SQLite、Compose 用 PostgreSQL 的数据库配置：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20527",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "DB_AUTO_MIGRATE": "true",
    "JWT_SECRET": "runtime-smoke-gb527-jwt-secret-at-least-32-bytes"
  },
  "ready_url": "http://127.0.0.1:20527/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

### 8. 部署、README 与端口

- Compose 名 `satellite-contact-window-deconfliction`；前端 `18527`、后端 `19527`、PostgreSQL `57527`，容器内 `80/8080/5432`。
- Compose 顶层 `name`、不含 `version:`，容器名带 `${COMPOSE_PROJECT_NAME:-satellite-contact-window-deconfliction}`；数据库使用命名卷，数据库、后端和前端均配置 healthcheck 并按健康状态依赖，`docker compose ps` 中所有长驻服务最终必须为 healthy。
- `.env`/示例包含 Compose 名、端口、数据库、JWT、CORS、默认评分权重和日志级别。Nginx `/api` 反代和 SPA fallback；中文目录可一键启动。
- README 中文、Docker 启动优先，含复制 env、地址、测试账号、功能、技术栈、目录、变量、API、枚举位置、算法假设、安全边界、本地开发、排错和 License。

### 9. 强制真实验收

1. 根目录运行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/project_scale.py .` 和 `python3 /Users/gaobo/.codex/skills/go-annotation-pipeline/scripts/runtime_smoke.py .`；规模实测必须为 2500-4200 行、24-42 个功能 `.go` 文件且低于 5000 行；前端完成依赖安装、类型检查和 `npm run build` 生产构建。
2. 运行 Compose config、构建启动、ps、健康/就绪检查，确认数据库/后端 healthy 和前端可访问。
3. 至少 8 项接口冒烟：登录、站点创建、卫星创建、窗口创建、冲突检测、锁定窗口保护、建议查询、接受方案、版本冲突、拒绝方案和审计查询；真实结果只写入 `output/execution.md`，不得写入 `runtime_smoke.json`。
4. 只用 Codex 内置 Browser 验证登录、5 页面、时间线、冲突证据、方案比较与权限显隐；禁止外部 Chrome，截图文件一律保存到 `output/`，Browser 步骤与结论只写入 `output/execution.md`。
5. 执行 `docker compose down -v --remove-orphans` 并确认本项目无容器残留。
6. 项目目录独立执行 `git init`，设置仓库本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。先提交除 `output/` 验证证据外的实现文件并取得实现 commit hash，将 hash 写入 `output/execution.md`，再把 `output/execution.md` 与截图作为第二次报告提交；最终 `git status --short` 为空。
